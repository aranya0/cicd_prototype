pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Building the project"'
                // Füge hier deine Build-Schritte ein (z. B. Build-Anweisungen für dein Projekt)
            }
        }
        stage('Test') {
            steps {
                sh 'echo "Testing the project"'
                // Füge hier deine Test-Schritte ein (z. B. Testanweisungen für dein Projekt)
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo "Deploying the project"'
                // Füge hier deine Deploy-Schritte ein (z. B. Deploy-Anweisungen für dein Projekt)
            }
        }
    }
}
